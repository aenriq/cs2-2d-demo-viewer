export const PLAYER_RADIUS = 10;
export const BEAK_LENGTH = 8;
export const BEAK_WIDTH = 10;
export const SHOT_LIFETIME_TICKS = 48;
/** Canvas stroke width for bullet tracers (pixels). */
export const SHOT_TRACER_WIDTH = 1;
export {
  DEFAULT_FRAME_TICK_INTERVAL,
  FRAME_TICK_INTERVAL,
  SUPPORTED_FRAME_TICK_INTERVALS,
} from "../utils/frame-interval.ts";
export const DEFAULT_CANVAS_SIZE = 1024;
